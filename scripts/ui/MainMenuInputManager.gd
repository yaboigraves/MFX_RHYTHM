extends Node

#so I dont really like the focus system its going to break quite bad
#so first lets listen for simple stuff like back presses
#and then this can get extended later
#the idea is that we want ui "states"
#oh duh a state machine
#lol
#ok so this can just issue events to the state machine
#overall the state machine controls literally all of the ui actual animations/triggers



# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
